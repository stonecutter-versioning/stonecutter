plugins {
    id "dev.kikugie.stonecutter"
}

stonecutter {
    centralScript = "build.gradle"
    kotlinController = false

    create(rootProject) {
        versions("1", "2")
        branch("subproject") {
            versions("1", "3")
        }
    }
}