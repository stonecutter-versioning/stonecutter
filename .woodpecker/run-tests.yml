labels:
  agent: homeserver

when:
  event: push
  branch: 0.*
  path:
    include:
      - "stitcher/**"
      - "stonecutter/**"
      - ".woodpecker/run-tests.yml"

steps:
  run-tests:
    image: gradle:jdk21-corretto
    commands:
      - gradle test --no-daemon -Dorg.gradle.configuration-cache.read-only=true